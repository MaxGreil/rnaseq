int threads = Runtime.getRuntime().availableProcessors()

int space = Runtime.getRuntime().maxMemory()

process {
    withName: BUILD_HISAT_INDEX {
        cpus = threads
        memory = space
    }
}
