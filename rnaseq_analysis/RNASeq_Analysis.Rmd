---
title: "RNASeq_Analysis"
author: "Maximilian Greil"
date: "2 1 2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
```

```{r}
# Load libraries

suppressMessages(library(DESeq2))
suppressMessages(library(tidyverse))
```

## SRA SRP055103

```{r}
data <- read_delim("featureCounts_output.txt.gz", delim="\t", skip=1)
data <- as.data.frame(data)

rownames(data) <- data[, 1]
data <- data[, -1]
data <- data[, c(6:11)]

colnames(data) <- gsub("\\.sorted.bam$", "", colnames(data))

data <- data[, order(names(data))]

data <- as.matrix(data)
head(data)

# Assign condition
condition <- factor(c("low", "low", "low", "high", "high", "high"), levels = c("high","low"))
```

```{r}


```